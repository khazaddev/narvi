# CLA signatures

This orphan branch stores `signatures/version1/cla.json`, written by the
CLA assistant workflow when a contributor signs. It intentionally shares
no history with `main` and holds no project code.

Do not protect this branch: the workflow must be able to commit to it.
