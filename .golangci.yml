version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - gocritic
    - revive
  exclusions:
    paths:
      - tools/lint/narvichecks/.*/testdata

formatters:
  enable:
    - gofmt
    - goimports
