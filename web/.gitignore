# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Regenerated by @tanstack/router-plugin on every `vite dev`/`vite build`
# from src/routes/**/*.tsx (see vite.config.ts's own comment) -- never
# hand-edited, and a fresh checkout never needs it committed.
src/routeTree.gen.ts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
