// Package ports will hold every application port interface (SandboxProvider,
// AgentRuntime, SourceControl, Notifier, IntentClassifier, LLM, BlobStore,
// SessionStore/TurnStore/SandboxStore, Outbox, TimerScheduler, Clock) —
// implemented incrementally starting PR-11/PR-12 (§4).
package ports
